# Supabase — copy to `.env.local` in this folder (`server/`) and replace [YOUR-PASSWORD].
# Prisma CLI reads `server/.env` by default: either copy these lines into `.env` or keep them
# only in `.env.local` and duplicate the two URL lines into `.env` before running migrations.

# Connect via connection pooling (PgBouncer — runtime / pooled queries)
DATABASE_URL="postgresql://postgres.nyayropgoimaanhpselw:mod243990cuf@aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.nyayropgoimaanhpselw:mod243990cuf@aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres"
